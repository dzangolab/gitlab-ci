.aws-ecr-login:
  script:
    - docker login -u AWS -p $(docker run --rm -e AWS_ACCESS_KEY_ID=$AWS_ACCESS_KEY_ID -e AWS_SECRET_ACCESS_KEY=$AWS_SECRET_ACCESS_KEY amazon/aws-cli ecr get-login-password --region $AWS_REGION) $REGISTRY
